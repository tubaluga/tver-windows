const ProfileGroupSection = ({ name }) => {
    return (
        <div className="border-b border-secondary-600">
            <p className="h1_sup text-secondary-700 pb-4">{name}</p>
        </div>
    );
};

export default ProfileGroupSection;
